name: learning_app
description: Language learning app with Firebase and AI features.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.6.0
  firebase_auth: ^5.3.4
  firebase_core: ^3.9.0
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.5.3
  flutter_tts: ^4.2.5
  flutter_svg: ^2.0.17
  rive: ^0.13.20
  speech_to_text: ^7.0.0
  flutter_animate: ^4.5.2
  confetti: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    # Generated by tools/build_hanzi_assets.py — do not edit by hand.
    # Declared as a directory so a newly generated file is bundled without
    # anyone having to remember to add it here.
    - assets/data/
    # OpenMoji (CC BY-SA 4.0, https://openmoji.org) — see assets/illustrations/CREDITS.md.
    - assets/illustrations/
    # rive-flutter example asset (MIT) — see assets/rive/CREDITS.md.
    - assets/rive/

  # Fonts are bundled rather than fetched at runtime: the app is designed to
  # work offline, and a network-dependent typeface would leave the first launch
  # rendering in a fallback face.
  #
  # "Mimi" identity type system — see assets/fonts/CREDITS.md.
  # Fredoka: display/headings/buttons. Nunito Sans: body text. Space Mono:
  # tags/stats/mono data.
  fonts:
    - family: Fredoka
      fonts:
        - asset: assets/fonts/Fredoka-500.ttf
          weight: 500
        - asset: assets/fonts/Fredoka-600.ttf
          weight: 600
        - asset: assets/fonts/Fredoka-700.ttf
          weight: 700
    - family: NunitoSans
      fonts:
        - asset: assets/fonts/NunitoSans-400.ttf
          weight: 400
        - asset: assets/fonts/NunitoSans-600.ttf
          weight: 600
        - asset: assets/fonts/NunitoSans-700.ttf
          weight: 700
    - family: SpaceMono
      fonts:
        - asset: assets/fonts/SpaceMono-400.ttf
          weight: 400
        - asset: assets/fonts/SpaceMono-700.ttf
          weight: 700
